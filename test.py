from palindrome import isPalindrome

strings = ['arara', 'Hannah', 'only', 'radar', 'level', 'amora', "A man, a plan, a canal – Panama"]

for string in strings:
    print(string, isPalindrome(strings))